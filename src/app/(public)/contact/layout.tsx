import {
    ThemeLayout,
    createMetadata,
} from "@/components/layout";

export const metadata = createMetadata({
    route: "/contact",
    routeName: "Contact",
});

export default ThemeLayout;
